apiVersion: apps/v1
kind: Deployment
metadata:
  name: motor
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: motor
  template:
    metadata:
      labels:
        app: motor
    spec:
      containers:
        - name: motor
          image: ghcr.io/TU_USUARIO/mancala-motor:v1.0.0   # tag inmutable en producción
          ports:
            - containerPort: 9000
          envFrom:
            - configMapRef:
                name: mancala-config
          resources:
            requests:
              cpu: "1"
              memory: "512Mi"
            limits:
              cpu: "4"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: 9000
            initialDelaySeconds: 3
            periodSeconds: 5